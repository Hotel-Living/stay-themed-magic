
export interface RoomType {
  id: string;
  name: string;
  description: string;
  capacity: number;
  basePrice: number;
}

export interface FormRoomType {
  name: string;
  description: string;
  capacity: number;
  basePrice: number;
}
